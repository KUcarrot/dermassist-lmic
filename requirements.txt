# ============================================================
# DermAssist LMIC - Dependency Requirements
# ============================================================
# Tested environment:
#   - Python 3.10.11
#   - CUDA 12.4
#   - GPU: RTX 4080 16GB
#   - OS: Windows 10/11
#
# Installation:
#   pip install -r requirements.txt
#
# For PyTorch with CUDA, the index URL below ensures CUDA 12.4 wheels.
# ============================================================

# --- PyTorch (CUDA 12.4) ---
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchvision==0.21.0

# --- Vision Models ---
timm==1.0.9                         # EfficientNet-B4 and other pretrained models
scikit-learn==1.5.2
numpy==1.26.4
pandas==2.2.3

# --- Image Preprocessing ---
opencv-python==4.13.0.92            # DullRazor hair removal, Grad-CAM overlay
Pillow==10.4.0
albumentations==1.4.16              # Medical image augmentation (training only)
scikit-image==0.24.0

# --- Gemma 4 LoRA Inference & Fine-tuning ---
transformers==5.5.4
peft==0.19.1                        # LoRA adapter loading
accelerate==1.13.0
bitsandbytes==0.49.2                # 4-bit NF4 quantization
datasets==4.8.4
trl==1.2.0                          # SFTTrainer (training only)
safetensors==0.7.0
tokenizers==0.22.2

# --- Synthetic Data Generation (training only) ---
diffusers==0.31.0                   # Stable Diffusion for minority class augmentation

# --- RAG (Retrieval-Augmented Generation) ---
sentence-transformers==3.1.1        # BAAI/bge-m3 embeddings
PyMuPDF==1.27.2.2                   # PDF text extraction for RAG knowledge base

# --- Gradio UI ---
gradio==6.13.0

# --- Evaluation & Reporting ---
matplotlib==3.9.2
seaborn==0.13.2

# --- Utilities ---
tqdm==4.66.6
python-dotenv==1.0.1                # .env file for HF_TOKEN

# --- Optional: Dataset Download ---
kaggle==1.6.17                      # Kaggle dataset download (HAM10000)
gdown==5.2.0                        # Google Drive download
